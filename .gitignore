node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# 下载的 B-roll 素材不入库（大文件，每个项目重下即可）
public/broll/*.mp4

# TTS 生成的配音不入库（跑 `npm run tts` 即可重新生成）
public/audio/*.m4a
public/case-audio/*.m4a

# 脚本生成的源代码（由 ingest/tts 自动生成，不上传）
src/case.generated.ts
src/caseTimings.generated.ts
src/timings.generated.ts

# 工作区元数据（WorkBuddy 项目配置，不上传）
.workbuddy
